import { ComplaintFilterPipe } from './complaint-filter.pipe';

describe('ComplaintFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ComplaintFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
